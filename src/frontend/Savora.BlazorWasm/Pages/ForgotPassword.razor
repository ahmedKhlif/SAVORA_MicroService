@page "/forgot-password"
@layout EmptyLayout
@inject IAuthService AuthService
@inject NavigationManager Navigation
@inject ISnackbar Snackbar
@inject IJSRuntime JSRuntime

<PageTitle>SAVORA - Mot de passe oublié</PageTitle>

<div class="main d-flex justify-content-center w-100 auth-page">
    <main class="content d-flex p-0">
        <div class="container d-flex flex-column">
            <div class="row vh-100">
                <div class="col-sm-10 col-md-8 col-lg-6 col-xl-5 mx-auto d-table h-100">
                    <div class="d-table-cell align-middle">
                        
                        <div class="text-center mt-4 mb-4">
                            <img src="img/logo.png" alt="SAVORA" height="60" class="mb-3" />
                            <h1 class="h2">Mot de passe oublié?</h1>
                            <p class="lead text-muted">
                                Entrez votre email pour recevoir un lien de réinitialisation
                            </p>
                        </div>

                        <div class="card shadow-lg">
                            <div class="card-body">
                                @if (_emailSent)
                                {
                                    <div class="text-center py-4">
                                        <div class="mb-4">
                                            <i class="align-middle text-success" data-feather="check-circle" style="width: 64px; height: 64px;"></i>
                                        </div>
                                        <h4 class="text-success">Email envoyé!</h4>
                                        <p class="text-muted mb-4">
                                            Si un compte existe avec cet email, vous recevrez un lien de réinitialisation.
                                            Vérifiez également votre dossier spam.
                                        </p>
                                        <a href="/login" class="btn btn-primary">
                                            <i class="align-middle me-1" data-feather="arrow-left"></i>
                                            Retour à la connexion
                                        </a>
                                    </div>
                                }
                                else
                                {
                                    <EditForm Model="@_model" OnValidSubmit="HandleSubmit">
                                        <DataAnnotationsValidator />
                                        
                                        <div class="mb-3">
                                            <label class="form-label">Adresse email</label>
                                            <InputText @bind-Value="_model.Email" class="form-control form-control-lg" placeholder="votre@email.com" />
                                            <ValidationMessage For="@(() => _model.Email)" class="text-danger" />
                                        </div>

                                        <div class="d-grid gap-2 mt-4">
                                            <button type="submit" class="btn btn-lg btn-primary" disabled="@_isLoading">
                                                @if (_isLoading)
                                                {
                                                    <span class="spinner-border spinner-border-sm me-2"></span>
                                                    <span>Envoi en cours...</span>
                                                }
                                                else
                                                {
                                                    <span>📧 Envoyer le lien</span>
                                                }
                                            </button>
                                        </div>
                                    </EditForm>
                                }

                                @if (!_emailSent)
                                {
                                    <div class="text-center mt-4">
                                        <a href="/login" class="text-muted">
                                            <i class="align-middle me-1" data-feather="arrow-left"></i>
                                            Retour à la connexion
                                        </a>
                                    </div>
                                }
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

@code {
    private ForgotPasswordModel _model = new();
    private bool _isLoading = false;
    private bool _emailSent = false;

    private async Task HandleSubmit()
    {
        _isLoading = true;
        try
        {
            var result = await AuthService.ForgotPasswordAsync(_model.Email);
            _emailSent = true;
            Snackbar.Add("Vérifiez votre email pour le lien de réinitialisation", Severity.Success);
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Erreur: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isLoading = false;
        }
    }

    public class ForgotPasswordModel
    {
        [System.ComponentModel.DataAnnotations.Required(ErrorMessage = "L'email est requis")]
        [System.ComponentModel.DataAnnotations.EmailAddress(ErrorMessage = "Email invalide")]
        public string Email { get; set; } = string.Empty;
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JSRuntime.InvokeVoidAsync("initFeatherIcons");
    }
}

